apiVersion: genesis.cluster.k8s.io/v1alpha1
kind: GenesisClusterProviderStatus
metadata:
  labels:
    controller-tools.k8s.io: "1.0"
  name: genesisclusterproviderstatus-sample
spec:
  # Add fields here
  foo: bar
