apiVersion: genesis.cluster.k8s.io/v1alpha1
kind: GenesisClusterProviderSpec
metadata:
  labels:
    controller-tools.k8s.io: "1.0"
  name: genesisclusterproviderspec-sample
spec:
  # Add fields here
  foo: bar
